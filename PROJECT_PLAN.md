# 联机对战后续项目计划

## 当前目标

把联机对战从“能跑的测试流程”推进到“规则稳定、可配置、可验证”的版本。优先保证双方看到的战斗逻辑一致，再逐步完善地图、迷雾、部署操作和 UI 表现。

## 核心规则方向

1. 房主在房间准备完成时生成并广播随机种子。
2. 双方使用同一份地图配置、同一个 seed、同一个 waveId 生成波次。
3. 迷雾部署阶段只同步操作，不展示对方本轮新部署。
4. 战斗阶段纯观看，双方单位、怪物、核心血量按同一套规则推进。
5. 每波结束后同步校验状态，再进入下一次迷雾部署。

## 优先级计划

### P0：确定性刷怪系统

- 目标：每波怪物由 `seed + waveId` 纯函数生成。
- 内容：怪物数量、类型序列、出怪间隔、左右路线分配都由规则生成。
- 当前实现：`waveId` 决定难度预算，seed 决定在预算内生成“少量强怪 / 多数弱怪 / 混合穿插”的分布，并按间隔释放。

### P1：地图配置与简易编辑器

- 目标：去掉代码硬编码地图坐标。
- 内容：美术提供底图，工具按网格标记 `Spawn`、`CoreA`、`CoreB`、`PathA`、`PathB`、`DeployA`、`DeployB`、`HighGround`、`Blocked`。
- 输出：JSON/CSV 地图配置，游戏启动时读取。

### P2：部署阶段操作闭环

- 目标：迷雾部署阶段支持部署、移动、升级、撤回。
- 内容：己方立即可见，对方本轮操作隐藏，战斗开始后统一公开。

### P3：同步模式升级

- 短期：保留 Host 权威快照，保证演示稳定。
- 中期：加入阶段开始/结束状态校验。
- 长期：改为确定性本地模拟，只同步输入、seed 和校验摘要。

### P4：视野和战争迷雾

- 目标：部署阶段只显示己方视野和上轮已公开信息。
- 内容：核心初始视野、单位攻击范围扩张视野、战斗阶段公开双方单位。

### P5：表现和调试清理

- 目标：降低日志噪音，强化 UI 可读性。
- 内容：血条、阵营颜色、核心扣血反馈、怪物出生提示、战斗结果提示。
- 当前实现：已加入投射物占位表现，卡牌/怪物攻击会显示弹道，后续可替换为图片资源。

## 当前正在处理

下一阶段建议从 P1 地图配置工具或 P4 视野扩张二选一推进；P0 确定性波次和 P5 投射物占位已具备可测试版本。
