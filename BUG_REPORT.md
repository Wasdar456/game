# Bug Report - 塔防游戏问题记录

> 记录时间：2026-05-23
> 测试分支：main
> 更新时间：2026-05-23 19:30

---

## 已修复问题

### ✅ 1. 攻击逻辑失效（已修复）
- **原因**：攻击范围使用曼哈顿距离计算，逻辑本身正确
- **实际问题**：单位部署位置离怪物路径太远，超出攻击范围
- **验证**：添加调试日志后确认攻击正常触发，血量确实在减少
- **相关提交**：添加了 DebugConfig.h 统一管理调试开关

### ✅ 2. 战斗退出功能（已修复）
- 在顶部状态栏添加了"✕ 退出"按钮
- 点击后弹出确认对话框
- 确认后清理战斗状态并返回主菜单

### ✅ 3. 网络房间提示（已修复）
- 创建房间后显示本机 IP 地址
- 格式：`IP 地址：192.168.x.x` + `端口号：9527`

---

## 待解决问题

## 1. UI 显示问题：元素首次需要鼠标 Hover 才显示

**问题描述：**
- 卡片选卡界面中，卡牌初始状态不显示，需要将鼠标移上去后才会显示
- 显示后即使鼠标移开也会保持显示状态
- 开始页面的选项按钮也存在同样问题
- 本质：**首次渲染时 UI 元素不可见，必须触发一次鼠标悬停事件才会显示**

**可能原因：**
- `paintEvent()` 中的绘制逻辑可能依赖某些状态变量，这些变量初始值不正确
- 可能缺少初始 `update()` 调用来触发首次绘制
- 样式表或透明度设置可能在初始化时有问题

**相关文件：**
- `src/ui/StartPage.cpp` - 开始页面
- `src/ui/DeckPage.cpp` - 选卡页面
- `src/ui/BattlePage.cpp` - 战斗页面

---

## 2. 怪物重叠问题：多个怪物在同一格导致血条难以分辨

**问题描述：**
- 默认每波生成 3+waveId 个怪物（第1波4个，第2波5个...）
- 所有怪物同时从同一出生点出发，路径相同，导致大量重叠
- 用户无法分辨单个怪物的血条变化
- 误以为攻击没有生效

**临时解决方案：**
- 已创建 `DebugConfig.h` 配置文件
- 修改 `MONSTERS_PER_WAVE = 1` 可让每波只出1个怪物

**长期解决方案：**
- 怪物生成后分散到相邻格子
- 或者添加出生间隔（每隔几秒出一个）
- 或者错开路径起点

**相关文件：**
- `src/core/base/DebugConfig.h` - 调试配置
- `src/core/systems/WaveSpawner.cpp` - 怪物生成逻辑

---

## 3. 联机模块状态分析

**整体架构（已完成）：**
- ✅ `GameServer` - Host 端服务器，监听端口，接受连接
- ✅ `GameClient` - Client 端客户端，连接服务器
- ✅ `LobbyManager` - 大厅状态机，管理握手流程
- ✅ `ProtocolDef.h` - 协议定义，包含所有消息类型
- ✅ `Serializer/Deserializer` - 数据包序列化/反序列化

**大厅握手流程（已实现）：**
1. Host 创建房间 → 监听端口 9527
2. Client 输入 IP 连接 → 发送 `JOIN_ROOM`
3. Host 收到后回复 `JOIN_ACK`
4. 双方点击"准备" → 发送 `PLAYER_READY`
5. 双方都准备好 → Host 广播 `GAME_START`（携带随机种子）
6. 进入选卡页面

**可以测试联机的玩法：**
- ✅ 创建/加入房间
- ✅ 大厅握手流程
- ✅ 随机种子同步
- ⚠️ 进入战斗后的同步（部分实现）

**联机战斗同步（部分实现）：**
- 已定义消息类型：`DEPLOY`, `MOVE_UNIT`, `UPGRADE_UNIT`, `WAVE_START` 等
- 已实现：`DeploymentSync`, `ClashSync`, `RandomSynchronizer`, `StateValidator`
- ⚠️ 但 UI 层（BattlePage）没有调用这些同步接口
- ⚠️ 战斗逻辑没有区分本地/远程操作

**相关文件：**
- `src/network/session/GameServer.cpp`
- `src/network/session/GameClient.cpp`
- `src/network/session/LobbyManager.cpp`
- `src/network/protocol/ProtocolDef.h`
- `src/network/sync/` - 同步模块

---

## 5. 网络功能使用方式不明确（已修复）

**问题描述：**
- 创建房间后，没有明确提示房主的 IP 地址或端口号
- 加入房间的人不知道应该输入什么信息

**已修复：**
- 创建房间后自动获取并显示本机局域网 IP 地址
- 显示格式：
```
>> 🏠 房间已创建！
>> ══════════════════════════════════════
>> 📋 请让对方输入以下信息加入房间：
>>    IP 地址：192.168.1.100
>>    端口号：9527
>> ══════════════════════════════════════
```

---

## 6. 设置页面功能有效性存疑

**问题描述：**
- 设置页面包含音量、分辨率、全屏、网格显示等选项
- 但不确定这些设置是否真正生效

**需要验证的功能：**
- [ ] BGM 音量滑块是否控制实际音量
- [ ] 音效音量滑块是否控制实际音量
- [ ] 分辨率切换是否生效
- [ ] 全屏切换是否生效
- [ ] "显示网格"复选框是否影响战斗页面

**相关文件：**
- `src/ui/SettingsPage.cpp` - 设置页面
- 需要检查信号是否正确连接到对应的处理函数

---

## 优先级建议

| 优先级 | 问题 | 状态 |
|--------|------|------|
| ✅ 已修复 | 攻击逻辑失效 | 已验证正常 |
| ✅ 已修复 | 战斗退出功能 | 已添加退出按钮 |
| ✅ 已修复 | 网络房间提示 | 已显示 IP 地址 |
| 🔴 高 | UI 首次不显示 | 待修复 |
| 🔴 高 | 怪物重叠问题 | 临时方案：DebugConfig |
| 🟡 中 | 联机战斗同步 | 部分实现，UI 未接入 |
| 🟢 低 | 设置功能验证 | 待验证 |

---

## 下一步行动

1. **修复 UI 显示问题** — 元素首次需要 Hover 才显示
2. **优化怪物生成** — 添加出生间隔或分散机制
3. **完善联机战斗** — 将同步模块接入 BattlePage
4. **验证设置功能** — 确认音量、分辨率等设置生效
